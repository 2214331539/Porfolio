cd frontend
npm run dev

cd backend
uv sync
uv run python run.py /uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

http://localhost:5173/admin
账号：admin
密码：admin123

更新部署：
cd /srv/portfolio
bash scripts/deploy.sh

ssh pp@121.196.165.152 'cd /srv/portfolio && bash scripts/deploy.sh'
